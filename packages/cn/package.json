{
  "name": "@kieranm/cn",
  "description": "Utilities for deduplicating, merging, and minifying CSS classnames",
  "private": true,
  "version": "1.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "TIMING=1 eslint --ext .js,.jsx,.ts,.tsx .",
    "lint:fix": "TIMING=1 eslint --fix --ext .js,.jsx,.ts,.tsx ."
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "tailwind-merge": "^2.5.2"
  }
}
